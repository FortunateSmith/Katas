function likes(names) {
  if (names.length === 0) {
    console.log( `no one likes this`);
  }
  for (let i = 0; i < names.length; i++) {
  if (names.length === 1) {
  console.log(`${i} likes this`);
  }
  if (names.lenght >= 2) {
    
  }


    }
}

likes(['Jacob', 'Alex'])
likes([]);
likes(['Erique']);